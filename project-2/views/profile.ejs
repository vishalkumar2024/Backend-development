<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 p-5 text-white">
        <div class="w-full  flex justify-end my-2">
            <a href="/logout" class="bg-red-500 px-3 py-2 rounded-md font-bold">Logout</a>
        </div>
        <div class="flex gap-3 item-center mb-3 ">
            <a href="/profilePic" class="size-10 rounded-full mt-[2px] overflow-hidden">
                <img class="w-full h-full object-cover " src="/uploadImages/<%=user.profilePicture%>" alt="">
            </a>
            <h2 class="text-[30px] font-bold ">Your profile - <%= user.name %>
            </h2>
        </div>

        <h5 class="mb-6 text-zinc-400">You can create a new post.</h5>

        <form action="/post" method="post">
            <textarea name="content" placeholder="What's on your mind?"
                class="resize-none border-2 border-zinc-700 w-1/3 p-3 outline-none  rounded"></textarea>
            <input type="submit" value="Create post"
                class="bg-blue-500 block px-6 mb-14 text-sm py-[7px] rounded-md my-3 ">
        </form>
        <h3>Your Posts</h3>
        <div class="flex gap-4 flex-wrap mb-3">

            <% user.post.reverse().forEach((item)=>{ %>
                <div class="bg-zinc-800 inline-block border-2 border-zinc-700 w-[32%] p-4 mt-5 rounded ">
                    <h4 class="text-blue-500 mb-2">
                        @<%= user.username%>
                    </h4>
                    <p class="tracking-tight text-white">
                        <%= item.content%>
                    </p>
                    <small class="inline-block mt-5">
                        <%= item.likes.length%> Likes
                    </small>
                    <div class="my-2 ">
                        <a href="/like/<%=item._id%>" class="text-green-500 mr-4 cursor-pointer">
                            <%= ( item.likes.indexOf(user._id)==-1)? "Like" :"Unlike" %>
                        </a>
                        <a href="/edit/<%= item._id %>" class="text-zinc-400 cursor-pointer mr-4">Edit</a>
                        <a href="/delete/<%= item._id %>" class="text-red-400 cursor-pointer">Delete</a>
                    </div>
                </div>
                <% })%>
        </div>
    </div>
</body>

</html>